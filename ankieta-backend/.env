# Database Config
DB_USER=viarin_admin
DB_PASSWORD=your_secure_password
DB_NAME=survey_db
DB_HOST=localhost
DB_PORT=5432

DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}